# Simple Nginx container with pre-built files
FROM nginx:alpine
WORKDIR /usr/share/nginx/html

# Copy pre-built files directly (build these locally first)
COPY ./dist ./

# Replace default nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
